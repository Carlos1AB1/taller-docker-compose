{
  "name": "task-manager",
  "version": "1.0.0",
  "description": "API de gestion de tareas con Docker",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "jest --forceExit --detectOpenHandles"
  },
  "dependencies": {
    "express": "^4.21.0",
    "mongoose": "^8.8.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
